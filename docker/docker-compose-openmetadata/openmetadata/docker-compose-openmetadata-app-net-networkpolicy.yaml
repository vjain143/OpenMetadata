apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  creationTimestamp: null
  name: docker-compose-openmetadata-app-net
spec:
  ingress:
    - from:
        - podSelector:
            matchLabels:
              io.kompose.network/docker-compose-openmetadata-app-net: "true"
  podSelector:
    matchLabels:
      io.kompose.network/docker-compose-openmetadata-app-net: "true"
